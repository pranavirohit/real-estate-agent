{
  "name": "nostos-rentals-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server exposing NYC rental search (Zillow via RapidAPI, with demo fallback) as structured tools for AI agents. Built for the Dedalus marketplace.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "provision": "tsx src/provision-machine.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^17.4.2",
    "express": "^4.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.21.0",
    "typescript": "^5.3.0"
  }
}
